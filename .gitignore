# --- Python build artifacts (never commit) ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- colcon / ROS 2 build outputs (generated by `colcon build`) ---
/build/
/install/
/log/

# --- Vendored third-party ROS packages (each is its own git checkout, not part
#     of this repo). Remove a line if you actually intend to vendor it here. ---
/isaac_ros_common/
/isaac_ros_object_detection/
/zed-ros2-wrapper/

# --- Debug artifacts: uncomment if you'd rather not track the overlay snapshots ---
# rtdetr_zed_tracker/phase2_overlay*.jpg
